const assert = require('assert');
const {expect} = require('chai');

const {sum, mult, numberIsPositive, numberIsNegative} = require(`../functions.js`);

describe( title: 'function sum', fn: ( => {
    assert.equal(sum(2,4)(6);
    expect(sum( 2,4 )). to.equal(6);
})
it(title: `should sum`)